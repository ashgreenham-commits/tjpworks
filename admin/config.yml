backend:
  name: git-gateway
  branch: main
```

And replace your `_headers` file with this:
```
/admin/*
  Content-Security-Policy: default-src 'self' 'unsafe-inline' 'unsafe-eval' https: data: blob:
  X-Frame-Options: SAMEORIGIN
